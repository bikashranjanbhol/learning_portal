# Fail fast if Node is older than package.json requires.
# Without this, npm installs happily and Next crashes later with a syntax
# error on modern JS, which reads like a bug in the code rather than an
# environment problem.
engine-strict=true
